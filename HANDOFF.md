# extra-keys presets + one-time switch card

Done: gate class ExtraKeysDefaultOffer (+ JVM test), prefs (offered flag + previous row per page),
strings, "Before the update" preset chip via presetsForPage(page, previous), the card in
TermuxActivity (two first-run-chain sites + onResume re-check) and takeDefaultExtraKeysRow.
Compile green; :app:testDebugUnitTest for terminal.io + tour green (321 tests, 0 failures).

Next: nothing outstanding — delete this file and make the final commit.

On: WIP commit on feat/extra-keys-presets, cut from dev 4418292e.
