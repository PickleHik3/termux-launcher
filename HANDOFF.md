Done: implemented recordedUser()/resolveUser() in ProotDistro.java, tests added.
In progress: running acceptance test.
Next: verify test output, commit final.
Gotchas: record file lives beside rootfs (entry dir), not inside it; GuiAppsSetup.RECORD_FILE_NAME reused.
