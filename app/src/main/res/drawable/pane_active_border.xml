<?xml version="1.0" encoding="utf-8"?>
<!-- Foreground border marking the focused pane in split-pane mode.
     Thin, rounded, uses the dynamic Material accent so it grounds in the theme palette. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="6dp" />
    <stroke
        android:width="1dp"
        android:color="?attr/termuxColorPrimary" />
</shape>
