<?xml version="1.0" encoding="utf-8"?>
<!-- The chosen card in a pick-one list (the speech model picker): the shell's rim in the primary
     role, the same colour as the radio it carries. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="?attr/termuxColorSurfacePanel" />
    <stroke
        android:width="1.5dp"
        android:color="?attr/termuxColorPrimary" />
    <corners android:radius="20dp" />
</shape>
