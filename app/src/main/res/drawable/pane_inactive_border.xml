<?xml version="1.0" encoding="utf-8"?>
<!-- Border for non-focused panes in split mode. Same thin rounded shape as the active border but
     the Material *secondary* tone (an actual accent hue, not grey), so inactive panes read as
     grounded in the theme while the active pane's primary-tone border still stands out. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="6dp" />
    <stroke
        android:width="1dp"
        android:color="?attr/termuxColorSecondary" />
</shape>
