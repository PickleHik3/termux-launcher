<com.termux.app.terminal.TermuxActivityRootView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_termux_root_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:clipChildren="false"
    android:clipToPadding="false">

        <FrameLayout
            android:id="@+id/terminal_root_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipChildren="false"
            android:clipToPadding="false">

        <!-- The wallpaper the launcher draws for itself, from the same captured frame every glass
             crop is cut from, so the two line up without guessing the ROM's composite zoom. Painted
             at the decor's rect (past this container's bounds, under the transparent system bars),
             and invisible whenever the system is drawing the wallpaper instead. -->
        <com.termux.app.chrome.WallpaperBackdropView
            android:id="@+id/wallpaper_backdrop"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:importantForAccessibility="no"
            android:visibility="invisible" />

        <FrameLayout
            android:id="@+id/terminal_status_bar_background"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_gravity="top"
            android:visibility="gone"
            android:background="?attr/termuxColorSurfaceBase">

            <!-- Wallpaper-mode frost: RealtimeBlurView cannot sample the system wallpaper behind
                 the transparent window, so this shows a crop of the dock's pre-blurred wallpaper
                 frame instead. Populated in updateTopPaneWallpaperFrost(). -->
            <ImageView
                android:id="@+id/terminal_status_bar_wallpaper_backdrop"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitXY"
                android:importantForAccessibility="no"
                android:visibility="gone" />

            <com.github.mmin18.widget.RealtimeBlurView
                android:id="@+id/terminal_status_bar_glass_blur"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:realtimeBlurRadius="10dp"
                app:realtimeOverlayColor="@android:color/transparent" />

            <View
                android:id="@+id/terminal_status_bar_glass_surface"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </FrameLayout>

        <RelativeLayout
            android:id="@+id/activity_termux_root_relative_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="0dp"
            android:layout_marginVertical="0dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="vertical">

        <LinearLayout
            android:id="@+id/terminal_content_column"
            android:layout_alignParentTop="true"
            android:layout_above="@+id/accessory_stack_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- The top edge's stack: every bar the place stands along the top, outermost first,
                 in the order EdgeStackPolicy.stack gives them. TermuxActivity.applyEdgeStacks()
                 re-parents them; what is declared inside here is only where they start. -->
            <com.termux.app.place.EdgeStackView
                android:id="@+id/place_edge_stack_top"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:edgeStackEdge="top"
                android:clipChildren="false"
                android:clipToPadding="false">

            <!-- App-owned top glass pane: the modular widget slot above the bottom-aligned
                 window row. The glass backdrop is extended behind the system status bar in
                 applyTerminalOverlayInsets(). -->
            <com.termux.app.statusbar.StatusBarSwipeLayout
                android:id="@+id/terminal_window_bar_host"
                android:layout_width="match_parent"
                android:layout_height="96dp"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:visibility="gone">

                <!-- Wallpaper-mode frost twin of terminal_status_bar_wallpaper_backdrop; both
                     crops come from the same shared blur frame so the seam is invisible. -->
                <ImageView
                    android:id="@+id/terminal_window_bar_wallpaper_backdrop"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="fitXY"
                    android:importantForAccessibility="no"
                    android:visibility="gone" />

                <com.github.mmin18.widget.RealtimeBlurView
                    android:id="@+id/terminal_window_bar_blur"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:realtimeBlurRadius="10dp"
                    app:realtimeOverlayColor="@android:color/transparent" />

                <View
                    android:id="@+id/terminal_window_bar_background"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />

                <!-- The lens edges and the place icons, behind everything the bar shows: the
                     neighbours rest half past the edges and the icon at home sits beside the
                     clock, none of it taking room from the row. Taps on a neighbour still land
                     here, since nothing above owns the bar's edges. -->
                <com.termux.app.statusbar.StatusBarLensView
                    android:id="@+id/terminal_status_lens"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />

                <!-- Stable modular slot: clocks render only here. A later widget editor can
                     resize/reorder this host without coupling widget code to the pane. The slot
                     owns the clock / media / pinned-notification contention; the pane height
                     never changes, content compresses instead.

                     Elevated above every glass view in this pane (backdrop, blur, surface) so
                     the clock is excluded from the dock's surface tuning: opacity, grain and
                     blur are the slab *behind* it and can never be composited over it, whatever
                     elevation a future glass pass gives those siblings (see the palette-glass
                     regression where setElevation reordered siblings, not just shadows).
                     outlineProvider=none keeps the lift from casting a shadow of its own. -->
                <com.termux.app.statusbar.TopPaneWidgetSlot
                    android:id="@+id/terminal_top_widget_area"
                    android:layout_width="match_parent"
                    android:layout_height="68dp"
                    android:layout_gravity="top"
                    android:elevation="6dp"
                    android:outlineProvider="none"
                    android:clipChildren="true"
                    android:clipToPadding="true">

                    <com.termux.app.terminal.TerminalClockWidget
                        android:id="@+id/terminal_clock_widget"
                        android:layout_width="wrap_content"
                        android:layout_height="match_parent"
                        android:contentDescription="@string/termux_top_pane_clock_content_description"
                        android:importantForAccessibility="yes" />

                    <com.termux.app.statusbar.PinnedNotificationsView
                        android:id="@+id/terminal_pinned_notifications"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/termux_top_pane_pinned_notifications_content_description"
                        android:visibility="gone" />

                    <com.termux.app.statusbar.MediaWidgetView
                        android:id="@+id/terminal_media_widget"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/termux_top_pane_media_content_description"
                        android:visibility="gone" />

                </com.termux.app.statusbar.TopPaneWidgetSlot>

                <!-- The clock a bar standing in a column wears, in place of the widget slot
                     above: the hour over the minutes with the date beneath. Shown only while
                     such a bar is open; lifted over the glass like the slot and the row. -->
                <com.termux.app.statusbar.StatusBarStackedClockView
                    android:id="@+id/terminal_status_column_clock"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="top"
                    android:elevation="6dp"
                    android:outlineProvider="none"
                    android:visibility="gone" />

                <!-- Bottom status row. Order is fixed: sessions indicator, window pills, then
                     the compact CPU/RAM/weather trailing widgets. The reserved rows above stay
                     empty until the next design pass. -->
                <!-- Lifted like the widget slot above: the status row's pills and widgets sit
                     over the pane's glass (backdrop, blur, surface) and must never be
                     composited into it, whatever elevation a glass pass gives those siblings.
                     outlineProvider=none keeps the lift from casting a shadow. -->
                <LinearLayout
                    android:id="@+id/terminal_status_row"
                    android:layout_width="match_parent"
                    android:layout_height="24dp"
                    android:layout_gravity="bottom"
                    android:layout_marginBottom="4dp"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:elevation="6dp"
                    android:outlineProvider="none"
                    android:clipChildren="true"
                    android:clipToPadding="true">

                    <!-- The place's own row content: its badge and its chips. The strip slides
                         with the pane wall and dissolves at the bar's edges, so one place's row
                         leaves through the lens and the next arrives through it, while the
                         stat widgets beside it hold still. -->
                    <com.termux.app.statusbar.PlaceContentStrip
                        android:id="@+id/terminal_status_place_content"
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:paddingStart="14dp"
                        android:paddingEnd="8dp"
                        android:gravity="center_vertical">

                        <com.termux.app.statusbar.SessionsIndicatorView
                            android:id="@+id/terminal_sessions_indicator"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent" />

                        <com.termux.app.terminal.TerminalWindowBar
                            android:id="@+id/terminal_window_bar"
                            android:layout_width="0dp"
                            android:layout_height="match_parent"
                            android:layout_weight="1"
                            android:layout_marginStart="6dp"
                            android:background="@android:color/transparent"
                            android:contentDescription="@string/termux_window_bar_content_description" />

                        <!-- The same window list for a bar standing in a column, where a pill
                             carrying a whole label does not fit: one chip per window, stacked.
                             Only one of the two is ever shown. -->
                        <com.termux.app.statusbar.StatusBarWindowColumn
                            android:id="@+id/terminal_status_window_column"
                            android:layout_width="match_parent"
                            android:layout_height="0dp"
                            android:layout_weight="1"
                            android:layout_marginTop="6dp"
                            android:visibility="gone"
                            android:contentDescription="@string/termux_window_bar_content_description" />
                    </com.termux.app.statusbar.PlaceContentStrip>

                    <!-- The CPU/RAM/weather cluster: CPU · RAM · Weather at the row's end
                         everywhere, except the Widgets place, where TermuxActivity reverses
                         the child order to Weather · RAM · CPU and shows the centring spacer
                         after it — see applyStatsClusterArrangement(). -->
                    <LinearLayout
                        android:id="@+id/terminal_status_stats_cluster"
                        android:layout_width="wrap_content"
                        android:layout_height="match_parent"
                        android:orientation="horizontal"
                        android:gravity="center_vertical"
                        android:paddingStart="3dp"
                        android:clipChildren="false"
                        android:clipToPadding="false">

                        <!-- No working indicator here: the window pill's own rim carries it, so
                             the state is shown once, at the window it belongs to. -->
                        <com.termux.app.statusbar.StatusBarWidgetView
                            android:id="@+id/terminal_status_widget_cpu"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:visibility="gone"
                            android:contentDescription="@string/termux_status_widget_cpu_content_description" />

                        <com.termux.app.statusbar.MaterialDotSeparatorView
                            android:id="@+id/terminal_status_dot_cpu_ram"
                            android:layout_width="3dp"
                            android:layout_height="3dp"
                            android:layout_gravity="center_vertical"
                            android:layout_marginHorizontal="1dp"
                            android:visibility="gone" />

                        <com.termux.app.statusbar.StatusBarWidgetView
                            android:id="@+id/terminal_status_widget_ram"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:layout_marginStart="3dp"
                            android:visibility="gone"
                            android:contentDescription="@string/termux_status_widget_ram_content_description" />

                        <com.termux.app.statusbar.MaterialDotSeparatorView
                            android:id="@+id/terminal_status_dot_ram_weather"
                            android:layout_width="3dp"
                            android:layout_height="3dp"
                            android:layout_gravity="center_vertical"
                            android:layout_marginHorizontal="1dp"
                            android:visibility="gone" />

                        <com.termux.app.statusbar.StatusBarWidgetView
                            android:id="@+id/terminal_status_widget_weather"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:layout_marginStart="3dp"
                            android:visibility="gone"
                            android:contentDescription="@string/termux_status_widget_weather_content_description" />
                    </LinearLayout>

                    <!-- Balances the place content strip's weight so the cluster above centres
                         in the row on the Widgets place; gone (so weightless) everywhere else,
                         where the cluster keeps hugging the row's end as it does today. -->
                    <View
                        android:id="@+id/terminal_status_stats_center_spacer"
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:visibility="gone" />

                    <!-- The stat widgets hold still while the place content slides; the end
                         margin keeps them clear of the lens and the icon peeking through it. -->
                    <LinearLayout
                        android:id="@+id/terminal_status_widgets"
                        android:layout_width="wrap_content"
                        android:layout_height="match_parent"
                        android:layout_marginEnd="10dp"
                        android:orientation="horizontal"
                        android:gravity="center_vertical"
                        android:paddingEnd="3dp"
                        android:clipChildren="false"
                        android:clipToPadding="false">

                        <!-- The AI glyph is not a preference: it appears only while a model is
                             actually resident, so it owns its own visibility and its own dot. -->
                        <com.termux.app.statusbar.MaterialDotSeparatorView
                            android:id="@+id/terminal_status_dot_ai"
                            android:layout_width="3dp"
                            android:layout_height="3dp"
                            android:layout_gravity="center_vertical"
                            android:layout_marginHorizontal="1dp"
                            android:visibility="gone" />

                        <com.termux.app.statusbar.StatusBarWidgetView
                            android:id="@+id/terminal_status_widget_ai"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:layout_marginStart="3dp"
                            android:visibility="gone"
                            android:contentDescription="@string/termux_status_widget_ai_content_description" />

                        <!-- Mouse mode's mark: there while it is on, gone the moment it is off. -->
                        <com.termux.app.statusbar.MaterialDotSeparatorView
                            android:id="@+id/terminal_status_dot_mouse"
                            android:layout_width="3dp"
                            android:layout_height="3dp"
                            android:layout_gravity="center_vertical"
                            android:layout_marginHorizontal="1dp"
                            android:visibility="gone" />

                        <com.termux.app.statusbar.StatusBarWidgetView
                            android:id="@+id/terminal_status_widget_mouse"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:layout_marginStart="3dp"
                            android:visibility="gone"
                            android:contentDescription="@string/termux_status_widget_mouse_content_description" />

                    </LinearLayout>
                </LinearLayout>

            </com.termux.app.statusbar.StatusBarSwipeLayout>

            <!-- The alphabets bar standing along the top, when the place on screen asks for
                 that. A row in the content column like the status bar's own, so it lands under
                 the status bar wherever that is on top and takes its slice of the terminal's
                 height between the side columns. syncAzBarHosts() sizes, glazes and fills it. -->
            <FrameLayout
                android:id="@+id/place_az_bar_host"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:visibility="gone">

                <!-- The glass sheet is exactly the plank, so it is the thing the rounded
                     outline clips; the host around it stays unclipped for the wave's lift. -->
                <FrameLayout
                    android:id="@+id/place_az_bar_host_glass"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <com.github.mmin18.widget.RealtimeBlurView
                        android:id="@+id/place_az_bar_host_blur"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        app:realtimeBlurRadius="10dp"
                        app:realtimeOverlayColor="@android:color/transparent" />

                    <View
                        android:id="@+id/place_az_bar_host_surface"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent" />
                </FrameLayout>
            </FrameLayout>

            <!-- One plank for a pinned-apps row lying down off the dock, and for the alphabets
                 index while it rides that row: both bars stand on this single sheet of glass
                 instead of carrying one each, so there is no seam between them and the whole plank
                 takes the dock's own corner radius. syncOffDockPlank() sizes and glazes it and
                 applyEdgeStacks() moves it, with the bars inside it in the same order the edge's
                 stack gives them. -->
            <RelativeLayout
                android:id="@+id/place_off_dock_plank_host"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:visibility="gone">

                <!-- The glass sheet is exactly the plank, so it is the thing the rounded outline
                     clips; the host around it stays unclipped for the wave's lift. It is pinned to
                     the bars rather than filling the host: a sheet that matched the host measured
                     the host, because a plain View under an at-most spec takes every pixel it is
                     offered, and the plank then swallowed the whole content column and left the
                     terminal nothing. The bars are the one thing with a height of its own, so the
                     host wraps them and the glass follows. -->
                <FrameLayout
                    android:id="@+id/place_off_dock_plank_glass"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_alignTop="@+id/place_off_dock_plank_bars"
                    android:layout_alignBottom="@+id/place_off_dock_plank_bars">

                    <com.github.mmin18.widget.RealtimeBlurView
                        android:id="@+id/place_off_dock_plank_blur"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        app:realtimeBlurRadius="10dp"
                        app:realtimeOverlayColor="@android:color/transparent" />

                    <View
                        android:id="@+id/place_off_dock_plank_surface"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent" />
                </FrameLayout>

                <!-- The bars on the plank, outermost first, turned by the edge the plank stands
                     on — the same stack the four edges use, so the order and the reversal are the
                     one piece of code. -->
                <com.termux.app.place.EdgeStackView
                    android:id="@+id/place_off_dock_plank_bars"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:edgeStackEdge="top"
                    android:clipChildren="false"
                    android:clipToPadding="false" />
            </RelativeLayout>
            </com.termux.app.place.EdgeStackView>

            <!-- The canvas band: the one horizontal row of the content column, holding the left
                 stack, the terminal and the right stack. A side rail therefore flanks the canvas
                 only — the top stack, the bottom stack, the dock's rows and the in-app keyboard
                 keep the whole width, the way the miniature has always drawn it. The terminal is
                 the weighted residual between the two stacks, so what EdgeStackPolicy.contentInsets
                 answers for the sides is structure rather than padding anyone applies. It clips:
                 a rail holds more icons than the screen is tall and scrolls them, and an unclipped
                 band let the scrolled ones out past the canvas to sit beside the dock. -->
            <LinearLayout
                android:id="@+id/terminal_canvas_band"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:orientation="horizontal"
                android:clipChildren="true"
                android:clipToPadding="true">

            <!-- The left stack. applyEdgeStacks() re-parents the bars between the two side stacks
                 and orders them; what is declared inside this one is only where they start. -->
            <com.termux.app.place.EdgeStackView
                android:id="@+id/place_edge_stack_left"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                app:edgeStackEdge="left"
                android:clipChildren="false"
                android:clipToPadding="false">

                <!-- The pinned apps standing off the dock: the one SuggestionBarView is lent to
                     this host whenever the row is not the dock's own, turned on its side for a left
                     or right edge. A ScrollView because a rail can hold more icons than the screen
                     is tall; syncPinnedAppsHost() sizes it and applyEdgeStacks() moves it. -->
                <com.termux.app.launcher.drawer.DockRailScrollView
                    android:id="@+id/place_apps_bar_host"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:fillViewport="true"
                    android:scrollbars="none"
                    android:overScrollMode="never"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:visibility="gone" />

                <!-- The extra keys standing off the dock: the toolbar pager's own first-page
                     ExtraKeysView is lent to this host. syncExtraKeysHost() fills and sizes it. -->
                <FrameLayout
                    android:id="@+id/place_extra_keys_host"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:clipToPadding="false"
                    android:visibility="gone" />

            </com.termux.app.place.EdgeStackView>

            <FrameLayout
                android:id="@+id/terminal_surface_host"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:clipChildren="false"
                android:clipToPadding="false">

                <View
                    android:id="@+id/terminal_background"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone"
                    android:background="?attr/termuxColorSurfaceBase" />

                <!-- The pane wall: fixed full-size places side by side, of which one is on
                     screen at rest. The terminal is the middle one and its host below is
                     untouched, so the terminal never resizes for the wall. The Widgets and
                     Display pages are added by their controllers when the install has them. -->
                <com.termux.app.wall.PaneWallLayout
                    android:id="@+id/terminal_pane_wall"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipChildren="false"
                    android:clipToPadding="false">

                    <!-- Split-pane host: TerminalPaneController inflates the recursive pane
                         tree (view_terminal_pane.xml per leaf) into this frame. A single
                         unsplit shell is just one pane filling the host. -->
                    <FrameLayout
                        android:id="@+id/terminal_pane_host"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:clipChildren="false"
                        android:clipToPadding="false" />
                </com.termux.app.wall.PaneWallLayout>

                <!-- Optional thin outline framing the terminal area; background/shape/margins
                     are set programmatically in applyTerminalBorderAppearance(). Draws the
                     shared glass rim instead of the plain stroke while the glass pane is on. -->
                <com.termux.app.TerminalGlassFrameView
                    android:id="@+id/terminal_border_overlay"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone" />
            </FrameLayout>

            <com.termux.app.place.EdgeStackView
                android:id="@+id/place_edge_stack_right"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                app:edgeStackEdge="right"
                android:clipChildren="false"
                android:clipToPadding="false" />
            </LinearLayout>

            <!-- The bottom edge above the dock, inside the content column so its bars stand on the
                 terminal's own height — which is exactly where a bottom status bar has always
                 been. The dock's own rows stand in accessory_row_stack instead, because that is
                 what keeps them above the in-app keyboard and on the dock's glass; the two
                 together are the bottom edge, and the walk splits the one stack between them. -->
            <com.termux.app.place.EdgeStackView
                android:id="@+id/place_edge_stack_bottom"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:edgeStackEdge="bottom"
                android:clipChildren="false"
                android:clipToPadding="false" />
        </LinearLayout>

        <RelativeLayout
            android:id="@+id/accessory_stack_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:visibility="gone">

            <FrameLayout
                android:id="@+id/accessory_surface_host"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_above="@id/inapp_keyboard_container"
                android:layout_alignWithParentIfMissing="true"
                android:clipChildren="true"
                android:clipToPadding="true"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/accessory_blur_backdrop"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="fitXY"
                    android:visibility="gone" />

                <View
                    android:id="@+id/accessory_top_edge_fx"
                    android:layout_width="match_parent"
                    android:layout_height="12dp"
                    android:layout_gravity="top"
                    android:visibility="gone" />

                <com.termux.app.DockLaunchRippleView
                    android:id="@+id/accessory_launch_ripple_underlay"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clickable="false"
                    android:focusable="false"
                    android:tag="dock_launch_ripple" />

                <View
                    android:id="@+id/extrakeys_background"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone"
                    android:background="?attr/termuxColorSurfacePanelHigh"/>

                <com.termux.app.DockEdgeGlowView
                    android:id="@+id/accessory_edge_glow_fx"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone" />

                <View
                    android:id="@+id/accessory_specular_fx"
                    android:layout_width="300dp"
                    android:layout_height="300dp"
                    android:layout_gravity="center"
                    android:visibility="gone" />
            </FrameLayout>

            <com.termux.app.LauncherAzGestureFxView
                android:id="@+id/apps_bar_az_fx_underlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_above="@id/inapp_keyboard_container"
                android:layout_alignWithParentIfMissing="true"
                android:clickable="false"
                android:focusable="false"
                android:visibility="gone" />

            <!-- The bottom edge's dock rows: the pinned apps, the alphabets index and the extra
                 keys, standing in the order EdgeStackPolicy.stack(BOTTOM) gives them. They used to
                 hang off each other by layout_above, which pinned them to the shipped order and
                 left every other one unrenderable. The stack is anchored above the keyboard
                 container and falls back to the parent's bottom when that is gone or has been
                 lifted out by the floating/split keyboard, which is the one thing the old chain's
                 alignWithParentIfMissing was there for.

                 Declared top to bottom in the shipped order, so the screen before the first
                 arrangement pass is the one the chain drew. Each row stands in a host of its own,
                 so the furniture that has to travel with it — the indicator band under the apps
                 row, the keybind strip in the letters' slot, the divider over the keys — moves
                 with it instead of anchoring to a sibling that is no longer beside it. -->
            <com.termux.app.place.EdgeStackView
                android:id="@+id/accessory_row_stack"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:edgeStackEdge="bottom"
                android:layout_above="@id/inapp_keyboard_container"
                android:layout_alignWithParentIfMissing="true"
                android:clipChildren="false"
                android:clipToPadding="false">

                <LinearLayout
                    android:id="@+id/apps_bar_row_host"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:clipChildren="false"
                    android:clipToPadding="false">

                    <FrameLayout
                        android:id="@+id/apps_bar_viewpager"
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:visibility="gone">

                        <!-- Transform host owned solely by the dock plank physics: the icon row
                             tilts and shifts with the glass under it. The bar itself keeps its own
                             page-switch transforms, and the pager keeps the drawer's lift
                             translation. -->
                        <FrameLayout
                            android:id="@+id/apps_bar_plank_layer"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:clipChildren="false"
                            android:clipToPadding="false" />
                    </FrameLayout>

                    <!-- The air between the icons and the letters. It belongs to the row, so it
                         travels with it rather than being a band of the stack in its own right. -->
                    <View
                        android:id="@+id/apps_bar_indicator_band"
                        android:layout_width="match_parent"
                        android:layout_height="3dp"
                        android:visibility="gone" />
                </LinearLayout>

                <FrameLayout
                    android:id="@+id/apps_bar_az_host"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:elevation="10dp"
                    android:clipChildren="false"
                    android:clipToPadding="false">

                    <com.termux.app.AzScrubRowView
                        android:id="@+id/apps_bar_az_row"
                        android:layout_width="match_parent"
                        android:layout_height="19dp"
                        android:gravity="center_vertical"
                        android:textColor="?attr/termuxColorOnSurfaceVariant"
                        android:elevation="10dp"
                        android:visibility="gone" />

                    <!-- Keybind hint row: takes the A-Z row's exact slot while a Ctrl+Alt prefix is
                         latched, so the hints cost no terminal space at all. Chips are built in
                         TermuxActivity.showKeybindHintDockRow(). -->
                    <HorizontalScrollView
                        android:id="@+id/keybind_hint_dock_row"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:elevation="11dp"
                        android:scrollbars="none"
                        android:clickable="true"
                        android:visibility="gone" />
                </FrameLayout>

                <FrameLayout
                    android:id="@+id/terminal_toolbar_host"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipChildren="false"
                    android:clipToPadding="false">

                    <androidx.viewpager.widget.ViewPager
                        android:id="@+id/terminal_toolbar_view_pager"
                        android:visibility="gone"
                        android:layout_width="match_parent"
                        android:layout_height="37.5dp"
                        android:background="@android:color/transparent"/>

                    <View
                        android:id="@+id/extrakeys_divider"
                        android:layout_width="match_parent"
                        android:layout_height="1dp"
                        android:layout_gravity="top"
                        android:layout_marginTop="5dp"
                        android:visibility="gone" />
                </FrameLayout>
            </com.termux.app.place.EdgeStackView>

            <com.termux.app.LauncherAzGestureFxView
                android:id="@+id/apps_bar_az_fx_overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_above="@id/inapp_keyboard_container"
                android:layout_alignWithParentIfMissing="true"
                android:clickable="false"
                android:focusable="false"
                android:visibility="gone" />

            <LinearLayout
                android:id="@+id/inapp_keyboard_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:orientation="vertical"
                android:visibility="gone">

                <LinearLayout
                    android:id="@+id/inapp_keyboard_height_adjust_controls"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="36dp">

                        <LinearLayout
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:layout_gravity="center"
                            android:gravity="center"
                            android:orientation="horizontal">

                            <TextView
                                android:id="@+id/inapp_keyboard_height_adjust_cancel"
                                android:layout_width="40dp"
                                android:layout_height="32dp"
                                android:background="?attr/selectableItemBackgroundBorderless"
                                android:contentDescription="@string/termux_in_app_keyboard_height_adjust_cancel"
                                android:gravity="center"
                                android:text="@string/termux_in_app_keyboard_height_adjust_cancel_symbol"
                                android:textColor="?attr/termuxColorOnSurface"
                                android:textSize="18sp" />

                            <FrameLayout
                                android:id="@+id/inapp_keyboard_height_adjust_handle"
                                android:layout_width="88dp"
                                android:layout_height="match_parent"
                                android:contentDescription="@string/termux_in_app_keyboard_height_adjust_handle"
                                android:clickable="true"
                                android:focusable="true">

                                <View
                                    android:id="@+id/inapp_keyboard_height_adjust_handle_indicator"
                                    android:layout_width="52dp"
                                    android:layout_height="4dp"
                                    android:layout_gravity="center"
                                    android:background="?attr/termuxColorOnSurfaceVariant" />
                            </FrameLayout>

                            <TextView
                                android:id="@+id/inapp_keyboard_height_adjust_confirm"
                                android:layout_width="40dp"
                                android:layout_height="32dp"
                                android:background="?attr/selectableItemBackgroundBorderless"
                                android:contentDescription="@string/termux_in_app_keyboard_height_adjust_confirm"
                                android:gravity="center"
                                android:text="@string/termux_in_app_keyboard_height_adjust_confirm_symbol"
                                android:textColor="?attr/termuxColorOnSurface"
                                android:textSize="18sp" />
                        </LinearLayout>
                    </FrameLayout>

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="28dp"
                        android:gravity="center_vertical"
                        android:orientation="horizontal"
                        android:paddingStart="8dp"
                        android:paddingEnd="8dp">

                        <TextView
                            android:id="@+id/inapp_keyboard_key_spacing_label"
                            android:layout_width="92dp"
                            android:layout_height="wrap_content"
                            android:maxLines="1"
                            android:text="@string/termux_in_app_keyboard_key_spacing"
                            android:textColor="?attr/termuxColorOnSurface"
                            android:textSize="11sp" />

                        <SeekBar
                            android:id="@+id/inapp_keyboard_key_spacing_slider"
                            android:layout_width="0dp"
                            android:layout_height="28dp"
                            android:layout_weight="1"
                            android:contentDescription="@string/termux_in_app_keyboard_key_spacing_slider"
                            android:max="200"
                            android:splitTrack="false" />
                    </LinearLayout>

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="28dp"
                        android:gravity="center_vertical"
                        android:orientation="horizontal"
                        android:paddingStart="8dp"
                        android:paddingEnd="8dp">

                        <TextView
                            android:id="@+id/inapp_keyboard_key_corner_radius_label"
                            android:layout_width="92dp"
                            android:layout_height="wrap_content"
                            android:maxLines="1"
                            android:text="@string/termux_in_app_keyboard_key_corner_radius"
                            android:textColor="?attr/termuxColorOnSurface"
                            android:textSize="11sp" />

                        <SeekBar
                            android:id="@+id/inapp_keyboard_key_corner_radius_slider"
                            android:layout_width="0dp"
                            android:layout_height="28dp"
                            android:layout_weight="1"
                            android:contentDescription="@string/termux_in_app_keyboard_key_corner_radius_slider"
                            android:max="240"
                            android:splitTrack="false" />
                    </LinearLayout>
                </LinearLayout>

                <FrameLayout
                    android:id="@+id/inapp_keyboard_suggestion_host"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:visibility="gone" />

                <FrameLayout
                    android:id="@+id/inapp_keyboard_view_host"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />
            </LinearLayout>
        </RelativeLayout>

        <!-- Where the keyboard goes when the place's keyboard type is Floating: the whole content
             region, so the frame inside it can be dragged anywhere and still be touchable, and
             deliberately not clickable, so everything outside the frame reaches the place under it.
             FloatingKeyboardController moves inapp_keyboard_container in here and back out. -->
        <FrameLayout
            android:id="@+id/floating_keyboard_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_alignParentBottom="true"
            android:clickable="false"
            android:focusable="false"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:elevation="14dp"
            android:visibility="gone" />

        <!-- Capture layer for the surface editor's surface taps and drag gestures. Sized in code to
             each surface's measured rect (expanded upward so the top-border handle is inside the hit
             area); the frame itself is not clickable, so touches outside the groups fall through.

             A tap on a group selects that surface, a horizontal drag walks its screen-edge margin,
             and the ring says which one the pill is pointing at. -->
        <FrameLayout
            android:id="@+id/surface_tuning_gesture_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_alignParentBottom="true"
            android:elevation="12dp"
            android:visibility="gone">

            <!-- The canvas is a surface, not empty space, so it takes taps too. First child, so the
                 three bounded surfaces above it win wherever their rects overlap this one: a
                 FrameLayout offers a touch to its children back to front. -->
            <FrameLayout
                android:id="@+id/surface_tuning_canvas_gesture_group"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:clickable="true"
                android:focusable="false"
                android:visibility="gone" />

            <FrameLayout
                android:id="@+id/surface_tuning_status_gesture_group"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:clickable="true"
                android:focusable="false"
                android:visibility="gone">

                <!-- The clock's own control: laid over the live clock in code while the status bar
                     is the surface being edited, with the ▾ drawn at its trailing edge. Clickable in
                     its own right: a child takes the touch before the group's select-and-drag
                     handler sees it. -->
                <TextView
                    android:id="@+id/surface_tuning_status_clock_handle"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="top|start"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:focusable="true"
                    android:gravity="center_vertical|end"
                    android:paddingEnd="8dp"
                    android:text="@string/termux_surface_tuning_clock_chevron"
                    android:textColor="?attr/termuxColorPrimary"
                    android:textSize="13sp"
                    android:visibility="gone" />
            </FrameLayout>

            <FrameLayout
                android:id="@+id/surface_tuning_dock_gesture_group"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:clickable="true"
                android:focusable="false"
                android:visibility="gone" />

            <FrameLayout
                android:id="@+id/surface_tuning_keyboard_gesture_group"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:clickable="true"
                android:focusable="false"
                android:visibility="gone" />

            <!-- One outline per surface the editor can act on, positioned in code to that
                 surface's own rect — not to its capture group, which overhangs it, and a ring drawn
                 on that would sit a finger's width above the thing it identifies.

                 With nothing picked they are all drawn and breathe together, which is the whole
                 invitation to touch one. With a surface picked its outline alone stands, steady,
                 and is what identifies the target; the card's own position never has to. -->
            <View
                android:id="@+id/surface_editor_ring_status"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:importantForAccessibility="no"
                android:visibility="gone" />

            <View
                android:id="@+id/surface_editor_ring_dock"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:importantForAccessibility="no"
                android:visibility="gone" />

            <View
                android:id="@+id/surface_editor_ring_keyboard"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:importantForAccessibility="no"
                android:visibility="gone" />

            <View
                android:id="@+id/surface_editor_ring_canvas"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="top"
                android:importantForAccessibility="no"
                android:visibility="gone" />

            <!-- Value echo for peeking controls. The pill fades while a control whose surface sits
                 under it is dragged, so the number it is changing has to appear somewhere the user
                 is already looking. Not clickable: it is a read-out over the live surface. -->
            <TextView
                android:id="@+id/surface_tuning_peek_readout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:background="@drawable/dock_tuning_done_pill"
                android:clickable="false"
                android:focusable="false"
                android:importantForAccessibility="no"
                android:paddingHorizontal="14dp"
                android:paddingVertical="7dp"
                android:textColor="?attr/termuxColorOnAccentContainer"
                android:textSize="13sp"
                android:textStyle="bold"
                android:visibility="gone" />
        </FrameLayout>

        <!-- Scrollback find strip: one text line above the dock, so the transcript it searches
             stays on screen with its matches lit. Filled by TerminalFindCoordinator. -->
        <FrameLayout
            android:id="@+id/terminal_find_bar_host"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@id/accessory_stack_container"
            android:layout_alignWithParentIfMissing="true"
            android:elevation="16dp"
            android:visibility="gone" />

        <!-- The surface editor. Not a card pinned above the accessory stack any more: an attached
             pill that parks next to whichever surface it is editing, carrying one property at a
             time. The host fills the region so the pill can be placed anywhere inside it, and is
             deliberately not clickable, so a touch outside the pill reaches the surface underneath
             and selects it. Elevation clears the gesture overlay, which the pill must sit over.

             Populated from surface_editor_pill.xml on the editor's first open. -->
        <FrameLayout
            android:id="@+id/surface_editor_pill_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_alignParentBottom="true"
            android:elevation="20dp"
            android:visibility="gone" />

        <!-- The Layout editor, the surface editor's sibling: a miniature of the place on screen,
             parked over the live place so the real thing stays visible behind it. The host fills
             the region and takes the touches the card does not, so a drag aimed at the miniature
             that lands beside it cannot reach the terminal underneath.

             Populated from layout_editor.xml on the editor's first open. -->
        <FrameLayout
            android:id="@+id/layout_editor_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_alignParentBottom="true"
            android:clickable="true"
            android:elevation="20dp"
            android:focusable="true"
            android:visibility="gone" />

        <com.termux.app.LauncherAzGestureFxView
            android:id="@+id/apps_bar_az_label_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_above="@id/accessory_stack_container"
            android:layout_alignWithParentIfMissing="true"
            android:clickable="false"
            android:focusable="false"
            android:visibility="gone" />
        </RelativeLayout>
        <!-- App drawer overlay plane. A full-bleed sibling of the padded content root, so it paints
             over the whole accessory stack while still losing to the command palette in z. It is
             deliberately NOT part of the accessory stack: the drawer animates with transforms only,
             never through the flush-padding solver or a terminal resize. INVISIBLE rather than gone
             so the host stays measured and the blur can gate its own work on isShown().
             AppDrawerController drives it and clips the glass to the animated rounded rect. -->
        <FrameLayout
            android:id="@+id/app_drawer_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:visibility="invisible">

            <FrameLayout
                android:id="@+id/app_drawer_glass"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- Wallpaper-mode frost: shows the shared pre-blurred wallpaper frame, clipped by
                     the plane's animated outline, replacing the live blur that can only see the
                     window's own (mostly transparent) content there. -->
                <ImageView
                    android:id="@+id/app_drawer_wallpaper_backdrop"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="fitXY"
                    android:importantForAccessibility="no"
                    android:visibility="gone" />

                <com.github.mmin18.widget.RealtimeBlurView
                    android:id="@+id/app_drawer_blur"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:realtimeBlurRadius="10dp"
                    app:realtimeOverlayColor="@android:color/transparent" />
            </FrameLayout>
        </FrameLayout>

        <!-- Command palette overlay. Last child of the content root so the palette can sprout
             from the in-app keyboard's space bar and land over the terminal without a Dialog
             window - a dialog would take focus and summon the system IME, which embedded-keyboard
             mode suppresses. INVISIBLE rather than gone so the host stays measured; the blur
             gates its own work on isShown(). TerminalCommandPaletteController drives it, and
             clips the blur to the animated rounded rect through the glass host's outline. -->
        <FrameLayout
            android:id="@+id/command_palette_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:visibility="invisible">

            <FrameLayout
                android:id="@+id/command_palette_glass"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="invisible">

                <!-- Wallpaper-mode frost: shows the shared pre-blurred wallpaper frame, clipped by
                     the glass pane's animated outline, replacing the live blur that can only see
                     the window's own (mostly transparent) content there. -->
                <ImageView
                    android:id="@+id/command_palette_wallpaper_backdrop"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="fitXY"
                    android:importantForAccessibility="no"
                    android:visibility="gone" />

                <com.github.mmin18.widget.RealtimeBlurView
                    android:id="@+id/command_palette_blur"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:realtimeBlurRadius="18dp"
                    app:realtimeOverlayColor="@android:color/transparent" />
            </FrameLayout>
        </FrameLayout>

        <!-- Terminal sheet plane. The one in-app surface every terminal prompt that used to be an
             AlertDialog or a PopupMenu now lands on. A dialog window takes focus, which pulls the
             InputConnection off TerminalView and summons the system IME — the swap the in-app
             keyboard exists to avoid, and what made the session browser's search box collapse the
             keyboard the moment it opened. Nothing in here is ever an EditText for the same reason;
             TerminalSheetController types the fields from the key channel.

             Above the palette in z because a sheet is modal and the palette collapses before it
             opens one, and below the rename chip because a rename can be started from a sheet.
             INVISIBLE rather than gone so the host stays measured and the blur gates its own work
             on isShown(). -->
        <FrameLayout
            android:id="@+id/terminal_sheet_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:visibility="invisible">

            <FrameLayout
                android:id="@+id/terminal_sheet_glass"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- Wallpaper-mode frost: shows the shared pre-blurred wallpaper frame, replacing
                     the live blur that can only see the window's own (mostly transparent) content
                     there. -->
                <ImageView
                    android:id="@+id/terminal_sheet_wallpaper_backdrop"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="fitXY"
                    android:importantForAccessibility="no"
                    android:visibility="gone" />

                <com.github.mmin18.widget.RealtimeBlurView
                    android:id="@+id/terminal_sheet_blur"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:realtimeBlurRadius="18dp"
                    app:realtimeOverlayColor="@android:color/transparent" />
            </FrameLayout>

            <!-- Cards, oldest first: a sheet opened from a sheet (the workspace picker's confirm)
                 stacks here rather than replacing the one under it. -->
            <FrameLayout
                android:id="@+id/terminal_sheet_stack"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipChildren="false" />
        </FrameLayout>

        <!-- Inline rename chips. Last child of the content root so a chip lands over the window bar
             or a pane without a Dialog window — a dialog would take focus and summon the system IME,
             which is exactly the swap renaming used to cost. Touch handling lives on the
             coordinator's listener: a tap outside the chip commits, except over the in-app keyboard,
             where it must fall through to the key being typed. Clickable would swallow those. -->
        <FrameLayout
            android:id="@+id/terminal_rename_chip_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="false"
            android:focusable="false"
            android:elevation="20dp"
            android:visibility="gone" />

    </FrameLayout>

    <FrameLayout
        android:id="@+id/activity_termux_bottom_space_view"
        android:layout_width="match_parent"
        android:layout_height="0dp">

        <View
            android:id="@+id/activity_termux_bottom_space_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:background="?attr/termuxColorSurfaceBase" />
    </FrameLayout>

</com.termux.app.terminal.TermuxActivityRootView>
