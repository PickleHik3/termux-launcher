<?xml version="1.0" encoding="utf-8"?>
<!-- Wrap, not fill. The browser is a panel on the terminal's foot: with three sessions in it a
     match_parent list stretched to the terminal's whole height and left most of the panel empty.
     The card's own top margin is the terminal's ceiling, so a long list still stops there and
     scrolls inside itself. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Not an EditText. The browser lives on the terminal sheet plane, which never takes focus so
         TerminalView keeps its InputConnection and no system IME is summoned; a real text field
         here would defeat that on the first tap. TerminalSheetController.TextField drives this
         label from the same key channel the palette and the rename chip are typed with. -->
    <TextView
        android:id="@+id/session_browser_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:gravity="center_vertical"
        android:minHeight="40dp"
        android:singleLine="true"
        android:textAppearance="?attr/textAppearanceBodyLarge" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/session_browser_new"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:minHeight="36dp"
            android:textSize="12sp"
            android:text="@string/session_browser_new" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/session_browser_clone"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:minHeight="36dp"
            android:textSize="12sp"
            android:text="@string/session_browser_clone_current" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/session_browser_save"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:minHeight="36dp"
            android:textSize="12sp"
            android:text="@string/session_browser_save_workspace" />
    </LinearLayout>

    <ListView
        android:id="@+id/session_browser_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbarStyle="outsideOverlay" />

    <TextView
        android:id="@+id/session_browser_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:gravity="center"
        android:text="@string/session_browser_empty"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:visibility="gone" />
</LinearLayout>
