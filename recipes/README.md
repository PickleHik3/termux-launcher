# Recipes have moved

The build recipes for the store's binaries (`cross/`, `termux/`, `nix/`) now live beside the
binaries they produce, in [PickleHik3/tlstore](https://github.com/PickleHik3/tlstore) under
`recipes/`, with their history. This pointer stays for one release and then goes.
