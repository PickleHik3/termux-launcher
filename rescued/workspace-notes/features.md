# next feature for Termux-launcher

## Home Screen Widgets

- long pressing on the in-app status bar (in expanded or compact forms) -> expands the status bar tow the bottom edge of the screen (respecting the padding set in-app) -> Clock becomes centered at the top edge (moves rightwards with animation)/ if there is media playing or pinned notifications then both are just aligned properly in the top row -> the newly made full screen expansion carries over the glass pane so the transisions are seamless -> the expanded pane will have a material (+) icon in the middle, touching it opens the add widget menu -> Top right corner of this new sheet will have a setting cog icon which will house the settings such as Rows x Columns (for defining widget grid) -> should support widget drag to move around, resize, long press to get into this mode (similar to established android launcher heuristics) -> when in this screen, back button to close the widgets area (slides up smoothly into the status bar previous form: if it was expanded prior to opening it then goes back to expanded, if it was compact, then goes back to compact).

## Dedicated App drawer

- Swiping straight down on the dock (Starting from the app icons row) -> opens up a full page app drawer with same styling as of the dock (also respecting the padding settings) -> animates smoothly, the dock raises up a little bit and expands upwards and downwards to fill the screen -> the Alphabets row sways like a loose rope and comes to rest on the right side of the popup window -> during the expansion animation, the in-app keyboard should be smoothly minimized (maintaining the current padding between the dock and in-app keyboard in rounded theme) (when default dock is active, the app icons row is cleanly seperated from the extrakeys and in-app keyboard, extra keys and in-app keyboard scrolls downward as one entity and disappaers and entire screen is filled by the app drawer) -> Search bar pill which matches the corner radius of the popup app drawer plane (maybe better to tie to the defined radius currently in the app as well) -> small settings cog at the bottom left corner of this popup -> App drawer view types: Vertical Grid (scroll up and down the app drawer), horizontal grid (paginated swipe left/right with dot page indicator at the bottom) -> category (apps automatically grouped into respective categories like Suggestions, Recently Added, Social, producttivity, Utilities, Entertainment, Shopping & Food, Finance, Health & Finance, Photo & Video, Travel, Information & Reading, Productivity & Finance, Other etc.) and uses vertical scroll to scroll through big matrix of app groups/categories, each category will be a rounded square (adhering to the app's/ app drawer pop-up's raidus) with 7 app icons visible in a single tile: 2 Rows, 1st Row has 2 big App icons of equal size and 2nd row will have 1 big app icon (same size as big app icons in the first row) and 4 smaller icons arranged into a 2x2 grid occupying the space of 1 big app icon. category heading will be under the tile (outside) and centered, Touching the small 2x2 app grid inside the tile or the row where the tile header will expand the tile to show entire apps (aligned towards bottom of the screen, big header of categroy just above the app icons (empty space above it), list expands upwards if there are more apps (unrealistic case but just in case: it can be scrollable) -> long pressing on app icons will need to bring up the simialr popup menu as currently shown when long pressing app icons in the app icons row in the dock. -> there should be option for setting app sizes, app drawer grid size for the horizontal/vertical scroll -> sliding fdinger up and down on the Alphabets column on the right should highlight the filtered app icons while making others transparent/ less attention. -> in vertical scroll view of app drawer when users flick down and the top rows of app is reacehd, it should show a spring animation, if users continue to swipe down (a second swipe down), the app drawer should minimize reversing the animation it showed while it was coming up. -> for horizontal scroll app drawer view, a deliberate swipe down closes the app drawer -> long pressing on an app icon and dragging it shoudl allow users to lift it up and drop it on top of another app icon to create an app folder, which once created will have its own popup, name (which can be toouched to rename) etc. -> during app drawer opening and close animations the pinned app icons should be smoothly animated to disappear on app drawer opening animation and appear when app drawer is closed animation.
