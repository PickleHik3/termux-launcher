# From https://gist.github.com/iainconnor/8605514
# with the addition of the /captures below.
/captures

# Built application files
build/
release/
*.apk
*.so
!app/src/main/jniLibs/
!app/src/main/jniLibs/arm64-v8a/
!app/src/main/jniLibs/arm64-v8a/libMNN.so
!app/src/main/jniLibs/arm64-v8a/libmnnllmapp.so
# The embedded X server, built by .github/workflows/build_x11_native.yml from the commit pinned
# in x11-server/UPSTREAM.md. x86_64 is there so the Display page can be tried on an emulator.
!app/src/main/jniLibs/x86_64/
!app/src/main/jniLibs/arm64-v8a/libXlorie.so
!app/src/main/jniLibs/x86_64/libXlorie.so
.externalNativeBuild
.cxx
*.zip

# Local configuration file (sdk path, etc)
local.properties

# Gradle generated files
.gradle/

# Signing files
.signing/

# Intellij
.idea/
*.iml

# Eclipse / Buildship
.classpath
.project
.settings/

# Vim
*.swo
*.swp

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.swp
ehthumbs.db
Thumbs.db

# Local QA snapshots
.qa/

# Agent documentation
project-plan.md
knowledge-base.md
CLAUDE.md
project-ledger.md

# Agent workspace (keep config/scripts tracked, ignore runtime logs)
/work-logs/build-error.log
/work-logs/build-last.log
/work-logs/build-debug.log
/work-logs/agent-log.md
/work-logs/*.tmp

# Reference repos
reference-repos/

# Design handoff bundle (reference only, not part of the app source)
/TL-handoff/

# Local agent / session scratch (do not commit)
.codex-tmp/
.codex-remote-attachments/
.omo/
*.mp4
WhatsApp Video*

# CLI-tool local state: agent worktrees, session scratch and settings all live
# under these, and none of it is part of the app source.
.claude/
.lavish/

# Reference-only handoff bundles (not part of app source)
.artifacts/
.gradle-codex-*/
